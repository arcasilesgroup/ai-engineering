"""Issue sync between local specs and external issue trackers.

Renamed from ``work_items`` per spec-132 D-132-03. The Pydantic manifest
schema still uses the ``work_items`` key (config-level wording shared
with provider hierarchy rules); only the Python module path moved.
"""
