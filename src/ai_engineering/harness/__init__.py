"""Deterministic context and safety harness (spec-194).

Read-only baseline collector that measures root-instruction bytes/tokens,
mandatory reads, discovered catalog metadata, duplicate identities, hook
injection cost, automatic writes and reachable MCP residue. Produces
redacted receipts; gates all follow-on specs (195-199).
"""
