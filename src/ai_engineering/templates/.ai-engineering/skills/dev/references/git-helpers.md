# Git Helpers

- Detect protected/default branch before destructive branch operations.
- Prefer safe deletion (`git branch -d`) and force delete only when remote tracking is gone.
- Validate clean worktree and upstream sync before branch cleanup.
- Use explicit branch targeting when deleting remote branches.
