# Mirror Authoring Contract

> Authoritative per-file authoring contract for the three root IDE mirrors
> (`AGENTS.md` / `CLAUDE.md` / `.github/copilot-instructions.md`) and the
> IDE-extras escape hatch. Extracted from `CANONICAL.md` §14 / §15 by spec-134
> sub-005 (§10.4 DRY — one canonical home). Mechanically-checkable rows are
> enforced by `tools/skill_lint/checks/md_mirror.py`.
>
> spec-201 D-201-04 collapsed the seven skill trees to two: `.claude/skills`
> (canonical, Claude Code only) and `.agents/skills` (the shared tree every
> other surface reads). Per-surface agent, command and hook trees did NOT
> collapse (D-201-22).

## Strict Content Contracts (per-file authoring reference)

The three root IDE mirrors carry byte-equivalent canonical payload (generated
from `src/ai_engineering/templates/project/CANONICAL.md`); each appends an
IDE-extras fence unique to that IDE. **Authoring contract** (brief §2.3 verbatim):

| File | MUST contain | MUST NOT contain |
|------|--------------|------------------|
| `<repo>/AGENTS.md` | Full canonical payload (§§0-13), no IDE-extras block. | Any `@AGENTS.md` import. Any IDE-specific runtime knobs. |
| `<repo>/CLAUDE.md` | Full canonical payload + IDE-extras with Hot-Path Discipline, Hooks Configuration, Runtime layer hooks, Token Efficiency, Engram doc pointer, Audit observability. | `@AGENTS.md` import. Cross-IDE skill list. |
| `<repo>/.github/copilot-instructions.md` | Full canonical payload + IDE-extras with Copilot Hooks Wiring table. | Any `See AGENTS.md` cross-reference (D-131-14). |
| `<repo>/.claude/skills/ai-<name>/SKILL.md` | (CANONICAL) The authored skill payload. Claude Code's search paths are compiled in, so it cannot read the shared tree. | Generated provenance frontmatter. |
| `<repo>/.agents/skills/ai-<name>/SKILL.md` | (ACTIVE per spec-201 D-201-04) The single shared skill tree read by Codex, Copilot, OpenCode, Cursor and Antigravity. Generated by `scripts/sync_mirrors/core.py` Surface 5d. | Hand-edited content. |
| `<repo>/.cursor/skills/*` | (DELETED per spec-201 D-201-04 — Cursor's discovery allowlist contains `.agents/skills/`, and the tree shipped 54 skill directories with zero `handlers/`, stopping `/ai-build` at preflight.) | The directory itself. |
| `<repo>/.cursor/agents/ai-<name>.mdc` | (ACTIVE) Agent payload files for Cursor. Generated. | Hand-edited content. |
| `<repo>/.opencode/skills/*` | (DELETED per spec-201 D-201-04 — the OpenCode binary reads `.agents/skills/`, and the tree also shipped zero `handlers/`.) | The directory itself. |
| `<repo>/.opencode/commands/ai-<name>.md` | (ACTIVE per spec-128 Wave 5) Thin saved-prompt wrappers (one per skill) that surface `/ai-<name>` in OpenCode's slash menu and lazy-load the matching skill via the `skill` tool. Generated by `scripts/sync_mirrors/opencode_target.py`. | Hand-edited content. Duplicated skill body bytes (SSOT is `.claude/skills/`). |
| `<repo>/.opencode/agents/ai-<name>.md` | (ACTIVE) Agent files for OpenCode. Generated at BOTH repo root and the install template by Surface 5b (spec-201 closed the root write-site gap). | Hand-edited content. |
| `<repo>/.codex/skills/*`, `<repo>/.codex/agents/*` | (DELETED per spec-201 D-201-04 / D-201-23 — Codex reads `.agents/skills` and registered every skill twice while it scanned both roots; its agent namespace accepts TOML only, so the 19 markdown files were inert.) | The directories themselves. |
| `<repo>/.github/skills/*`, `<repo>/.github/chatmodes/*` | (DELETED per spec-201 D-201-04 — Copilot reads `.agents/skills`; the chatmode alias had no generator write site.) | The directories themselves. |
| `<repo>/GEMINI.md` / `<repo>/.gemini/*` | (DELETED per spec-151 — Gemini CLI support retired pre-release.) | The files/directories themselves. |
| `<repo>/.codex/AGENTS.md` | (DOES NOT EXIST — Codex reads root AGENTS.md natively.) | The file itself. |
| `<repo>/.github/instructions/*` | (DELETED per spec-128 D-128-07 — copilot-instructions.md covers it.) | The directory itself. |
| `<repo>/.agent/*` | (DELETED per spec-151 — Antigravity uses `.agents/`.) | The directory itself. |
| `<repo>/.cursor/rules/*` | (NOT USED for skills — Cursor reads the shared `.agents/skills/` tree. Operators may add their own always-included rules here without conflict.) | Framework-generated content. |
| `<repo>/CONSTITUTION.md` | Project-identity only (10 sections: Mission / Stakeholders / Vocabulary / Prohibitions / Compliance gates / Anti-goals / Boundaries / Escalation / Language / Lifecycle phase). | Any AI-behaviour header (KISS / YAGNI / SOLID / DRY / TDD / SDD / Clean Code / Hexagonal Architecture / Simplicity First / Plan-Mode Default / Surgical Changes / Goal-Driven Execution / Subagent Strategy / Self-Improvement Loop / Demand Elegance / Autonomous Bug Fixing / Think Before Coding). |
| `<repo>/README.md` | Install, value-prop, links to AGENTS.md + CONSTITUTION.md. | Skill list. Agent list. Chain duplication. |
| `<repo>/CONTRIBUTING.md` | Dev setup, PR process, test commands, repo layout. | Duplication of canonical content. |
| `<repo>/docs/getting-started.md` | 3-minute path: install → `/ai-start` → first `/ai-brainstorm` → first PR. | Internals. Skill list. |

Mechanically-checkable rows are enforced by `tools/skill_lint/checks/md_mirror.py`:

- sha256 equivalence across the three root mirrors (canonical payload
  bytes, fence stripped).
- No `@AGENTS.md` import in any mirror.
- No `GEMINI.md` or `.gemini/` orphan on disk.
- No `.codex/AGENTS.md` orphan on disk.
- No forbidden AI-behaviour header in CONSTITUTION.md.
- No extracted section headings (`## 10.` / `## 14.` / `## 15.` /
  `## 16.`) in any mirror (sub-005 guard).
- The three `docs/` targets (`principles.md`, `mirror-authoring.md`,
  `surface-axioms.md`) exist on disk (sub-005 guard).

## IDE-Extras Escape Hatch (R-1.1 / R-131-02 mitigation)

Each IDE mirror MAY append a fenced block carrying content unique to that IDE
(Hot-Path tuning for Claude Code, Hooks Wiring table for Copilot, etc.).
`md_mirror.py` recognises the fence and strips it before the sha256 equivalence
check.

```
<!-- ide-extras:start -->
…IDE-specific content (hot-path budgets, hooks wiring,
runtime tunables)…
<!-- ide-extras:end -->
```

The block is OPTIONAL — `AGENTS.md` (the base mirror) carries no extras; all
other mirrors carry at most one fence block.
