# Manual Branch Policy Setup (GitHub)

1. Open repository settings -> Branches.
2. Create a rule for `main`.
3. Require pull request before merging.
4. Require status checks: `ci`, `ai-pr-review`, `ai-eng-gate`.
5. Restrict direct pushes.
