---
name: ai-architecture
description: "Architecture analysis — drift detection, coupling, cohesion, boundaries, tech debt. Includes compatibility mode. Runs in isolated context."
argument-hint: "drift|coupling|boundaries|compatibility"
---

Read and execute the skill defined in `.ai-engineering/skills/architecture/SKILL.md`.

Use context:fork for isolated execution when performing heavy analysis.

Modes: `architecture` (default), `compatibility` (backwards compatibility analysis).

$ARGUMENTS
