---
id: sub-003
parent: spec-127
milestone: M2
title: "Description CSO pass + Examples + Integration"
status: planning
files:
  - .claude/skills/**/SKILL.md
  - .claude/agents/**/*.md
  - .github/skills/**
  - .codex/skills/**
  - .gemini/skills/**
depends_on:
  - sub-001
  - sub-002
---

# Sub-Spec 003: M2 — Description CSO pass + Examples + Integration

## Scope

Apply the Anthropic CSO (Concise / Specific / Outcome-oriented) third-person
description pass over all 50 current skills (final 46 post-merger). Wave A:
Grade D + bottom-10 (10 skills). Wave B: Grade C cluster (6 skills). Wave C:
Grade B (14 skills). Wave D: Grade A (28 skills, polish only). Append
`## Examples` section (≥2 invocations) to every skill. Append `## Integration`
section linking adjacent skills. Apply parallel agent rubric pass: every
agent file gets frontmatter `description` (CSO third-person), explicit `tools`
whitelist, `model` declared (`opus` or `sonnet`), dispatch-source comment.
Re-run `skill_lint --check`; expected zero Grade D, ≤2 Grade C. Verify
`tests/conformance/test_skills_rubric.py` and `test_agents_rubric.py` green.

## Exploration

### Surface inventory

- 50 SKILL.md under `.claude/skills/<name>/SKILL.md` (sum 5,823 lines).
- 26 agent files under `.claude/agents/*.md`.
- Mirror trees: `.github/skills/`, `.codex/skills/`, `.gemini/skills/`
  (regenerated by `sync_command_mirrors.py` at end of milestone).

### Dependencies

- sub-001 (M0): spec_lifecycle, AGENTS.md/CLAUDE.md voice updates — provides
  the doc baseline this milestone references when adding `## Integration` links.
- sub-002 (M1): `tools/skill_lint --check` + `--baseline` shipped; conformance
  tests RED at baseline grade — this milestone drives them GREEN at M2 bar
  (zero D, ≤2 C). `docs/conformance-report.md` baseline already committed.

### Skill grade map (from brief §2.1)

**Wave A — Grade D + bottom-10** (10 skills, P0 fix per brief §2.4):
ai-entropy-gc (sole D), ai-instinct, ai-mcp-sentinel, ai-canvas, ai-eval,
ai-run, ai-platform-audit, ai-governance, ai-skill-evolve, ai-constitution

**Wave B — Grade C cluster** (6 skills): ai-cleanup, ai-pipeline,
ai-board-discover, ai-board-sync, ai-support, ai-resolve-conflicts (note:
ai-mcp-sentinel + ai-instinct deduped into Wave A)

**Wave C — Grade B** (14 skills): ai-verify, ai-test, ai-review, ai-write,
ai-docs, ai-research, ai-start, ai-commit, ai-pr, ai-debug, ai-standup,
ai-learn, ai-explain, ai-board-sync

**Wave D — Grade A** (~28 skills, polish-only): ai-plan, ai-prompt, ai-design,
ai-animation, ai-postmortem, ai-autopilot, ai-brainstorm, ai-code, ai-create,
ai-dispatch, ai-guide, ai-market, ai-media, ai-note, ai-release-gate,
ai-schema, ai-security, ai-slides, ai-sprint, ai-video-editing,
ai-analyze-permissions + post-Wave-A re-grades. Final composition determined
by `skill_lint --check` between Wave C and Wave D.

### Agent rubric pass

26 agent files; rubric requires:
- Frontmatter `description` rewritten CSO third-person
- Explicit `tools` whitelist
- `model` declared (`opus` or `sonnet`)
- Dispatch-source comment: `<!-- dispatched-by: /ai-review,/ai-verify -->`
- No-orphan: orphans flagged for M4 deletion: `reviewer-design`,
  `ai-run-orchestrator`

### Hot-path impact

None. M2 edits are pure markdown frontmatter + body sections.
`tests/perf/test_hot_path_budgets.py` remains green by construction.

### Architecture

Domain-only edits. M2 touches `.claude/skills/**/SKILL.md` and
`.claude/agents/**/*.md` — *domain* artifacts in the hexagonal split. Mirrors
are *infra* — regenerated once at end of milestone, not hand-edited per wave.
