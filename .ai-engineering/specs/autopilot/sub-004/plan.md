---
total: 4
completed: 4
---

# Plan: sub-004 Improvement Funnel - Proposals + Work Items

## Plan

exports: ["proposals.md format", "--review steps 4-5 procedure in ai-instinct SKILL.md"]
imports: ["instincts.yml v2 schema (sub-002)", "--review base procedure (sub-003)", "LESSONS.md path (sub-001)"]

- [x] T-4.1: Define proposals.md format and creation logic in SKILL.md
  - **Files**: `.claude/skills/ai-instinct/SKILL.md`
  - **Done**: Step 4 (EVALUATE) documented in --review procedure: filter instincts (confidence >= 0.7, evidenceCount >= 3), read LESSONS.md for cross-reference, read project context (CONSTITUTION.md, target SKILL.md/agent.md), assess target artifact (skill/agent/hook/new), write PROP-NNN entry to proposals.md with evidence, diagnostic, proposed fix, status.

- [x] T-4.2: Define work item creation logic in SKILL.md
  - **Files**: `.claude/skills/ai-instinct/SKILL.md`
  - **Done**: Step 5 (CREATE WORK ITEMS) documented: read manifest.yml work_items.provider. If GitHub: `gh issue create --title "instinct: [target] - [diagnostic]" --body "[proposal]" --label "ai-engineering,instinct"`. If Azure DevOps: `az boards work-item create --type "Bug" --title ... --description ...`. Check duplicates first: `gh issue list --label "ai-engineering,instinct" --state open --json title`. Update proposals.md status to work-item-created with ref.

- [x] T-4.3: Create initial proposals.md with header
  - **Files**: `.ai-engineering/instincts/proposals.md`
  - **Done**: File created with header: `# Improvement Proposals\n\nGenerated by /ai-instinct --review. Each proposal is backed by instinct evidence + LESSONS.md cross-reference.\n`. No entries yet (populated by runtime).

- [x] T-4.4: Add proposals.md to ownership rules
  - **Files**: `src/ai_engineering/state/defaults.py`
  - **Done**: Entry added: `(".ai-engineering/instincts/proposals.md", OwnershipLevel.SYSTEM_MANAGED, FrameworkUpdatePolicy.ALLOW)`. System-managed because it's generated by the framework, not team-edited.

### Confidence
- **Level**: medium
- **Assumptions**: `gh` CLI and `az` CLI are available in the environment. ai-board-sync pattern is stable and reusable.
- **Unknowns**: Whether instinct-generated issues should use a different label than "ai-engineering,instinct" or reuse existing labels from manifest.

## Self-Report

**Status**: COMPLETE (4/4 tasks)

### What was done

- **T-4.1**: Enriched Step 4 (EVALUATE) in ai-instinct SKILL.md. Added explicit reads of CONSTITUTION.md, manifest.yml, and target artifact (SKILL.md/agent.md) for project context. Introduced PROP-NNN sequential numbering with Status, Source, Pattern, Diagnostic, Proposed fix, Target, and LESSONS.md cross-ref fields. Added dedup check on existing proposals before assigning next number.
- **T-4.2**: Enriched Step 5 (CREATE WORK ITEMS) in ai-instinct SKILL.md. Added duplicate check step before creation (gh issue list / az boards query). Aligned labels to "ai-engineering,instinct". Added post-creation update to proposals.md (status -> work-item-created, work item ref). Explicit fail-open language matching ai-board-sync protocol. Title format: "instinct: [target] - [diagnostic]".
- **T-4.3**: Created `.ai-engineering/instincts/proposals.md` with header only. No entries (runtime-populated).
- **T-4.4**: Added proposals.md ownership entry to `src/ai_engineering/state/defaults.py` as SYSTEM_MANAGED + ALLOW, placed after meta.json in the instincts group.

### Decisions

- Used `--type "Task"` (not "Bug") for Azure DevOps work items since instinct proposals are improvements, not defects. Consistent with sub-003 plan.
- Proposal format uses PROP-NNN numbering for stable references between proposals.md and work items.
- LESSONS.md cross-ref field enables skip-if-already-captured logic and partial-overlap awareness.

### Validation

- `ruff check` + `ruff format --check` passed on defaults.py.
- SKILL.md Steps 4-5 verified complete against plan requirements.
