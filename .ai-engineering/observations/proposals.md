# Improvement Proposals

Generated by `/ai-skill-improve --review`. Each proposal is backed by instinct evidence cross-referenced with LESSONS.md.

## PROP-001: ai-build/ai-pr deliver must detect user-prohibited PR creation and switch to update-existing-PR mode

- **Status**: proposed
- **Source**: corrections instinct, confidence 0.7, evidence 3
- **Pattern**: When the user explicitly says "stay on this branch / use the open PR / no new branch or PR" while invoking /ai-build or /ai-pr, the deliver phase must NOT create a new PR — it must stay on the active branch, push, and update the existing PR via `gh pr edit` (retitle + append `## Additional Changes`).
- **Diagnostic**: 3rd occurrence (2026-06-04: "esta rama y el pr abierto" → built spec-165 onto fix/soul-md-install-wiring + PR #586; priors: spec-128, spec-131). Default deliver assumes new-PR creation each run.
- **Proposed fix**: ai-build `handlers/deliver.md` + ai-pr Step 14 — pre-deliver check for an existing open PR on the current branch AND honor an operator "no new branch/PR" constraint; default to update-mode when either holds.
- **Target**: SKILL.md (ai-build deliver handler) + SKILL.md (ai-pr Step 14)
- **LESSONS.md cross-ref**: partial — "Existing PR updates must reconcile branch spec history" (line 39) covers _history reconciliation, NOT the deliver-mode switch.
