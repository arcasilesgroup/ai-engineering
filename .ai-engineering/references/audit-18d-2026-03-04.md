# ai-engineering 18-Dimension Audit Report

**Date**: 2026-03-04 | **Framework**: 0.2.0 (manifest) / 0.1.0 (package) | **Branch**: `chore/v3-refinements`
**Scope**: 122 py + 201 gov + 79 test (1,416 tests) files | **Model**: claude-opus-4-6 + claude-sonnet-4-6

---

## Executive Summary

| Metric | Value |
|--------|-------|
| **Overall Score** | **68 / 100** |
| **Verdict** | **NO-GO for release** |
| **Release Readiness** | 1 P0 blocker, 6 P1 issues, 13 P2 advisories |
| **Maturity Level** | Late Beta — core enforcement solid, agentic model partially backed, docs stale |

### Dimension Scores

| # | Dimension | Verdict | Score |
|---|-----------|---------|-------|
| D2 | Contract Compliance | WARN | 60 |
| D3 | Feature Inventory | PASS | 93 |
| D4 | Code Value | PASS | 88 |
| D5 | Docs/YAML/JSON | WARN | 55 |
| D6 | Backlog Architecture | WARN | 72 |
| D7 | Command E2E | FAIL | 40 |
| D8 | Security Enforcement | WARN | 75 |
| D9 | Ownership Safety | WARN | 70 |
| D10 | Template Drift | WARN | 78 |
| D11 | State Integrity | WARN | 72 |
| D12 | Test Coverage | WARN | 68 |
| D13 | Complexity Reduction | WARN | 70 |
| D14 | Release Gate | NO-GO | 45 |
| D15 | Cross-OS | WARN | 72 |
| D16 | Installer/Doctor | WARN | 75 |
| D17 | Maintenance | WARN | 65 |
| D18 | Architecture Drift | WARN | 75 |

**Distribution**: 2 PASS, 14 WARN, 1 FAIL, 1 NO-GO

---

## Top 10 Cross-Cutting Findings

| # | Finding | Dimensions | Severity |
|---|---------|-----------|----------|
| 1 | **Gitleaks wrong command** in `workflows.py:225` — `detect --staged` instead of `protect --staged` | D7, D8, D14 | P0 |
| 2 | **README.md substantially stale** — shows v2 counts (47 skills, 6 agents vs actual 34/7) | D5, D10, D17 | P1 |
| 3 | **Version inconsistency** — `install-manifest.json` at 0.1.0/schema 1.1 vs `manifest.yml` at 0.2.0/model 1.2 | D5, D11, D14 | P1 |
| 4 | **Documentation gate absent** from commit/PR workflows — CHANGELOG/README never auto-checked | D7, D2 | P1 |
| 5 | **Ownership-map.json stale** — missing `.github/prompts/**`, `.github/agents/**`, `.claude/**` | D9, D10 | P1 |
| 6 | **Tool-availability code duplicated 3×** across detector/readiness.py, doctor/checks/tools.py, installer/tools.py | D4, D13, D16 | P1 |
| 7 | **Backward-compat shims** — gates.py `__getattr__` (65L) + doctor/service.py wrappers (80L) = 145L dead indirection | D4, D13 | P2 |
| 8 | **Test pyramid skewed** — 69/29/2% vs target 50/45/5%; 6 test stubs; StateService untested | D12 | P2 |
| 9 | **No automated maintenance schedule** — manifest declares weekly cadence, no CI cron exists | D17 | P2 |
| 10 | **Contract compliance at 45% strict PASS** — 4 FAIL clauses (empty-repo wizard, commit format, branch naming, remote-skills default) | D2 | P2 |

---

## Wave 1 — Inventory & Integrity

### D3: Feature Inventory & Capability Map — PASS (93)

**Summary**: All declared features are implemented and working.

| Category | Declared | Implemented | Tested | Working |
|----------|----------|-------------|--------|---------|
| CLI Commands | ~35 | 50 | 49 | 44 working + 6 undeclared |
| Agents | 7 | 7 | N/A (prompt docs) | 7 |
| Skills | 34 | 34 | structural | 34 |

- 6 undeclared CLI subcommands (guide, gate commit-msg, gate risk-check, maintenance branch-cleanup/risk-status/pipeline-compliance/repo-status/spec-reset, setup azure-devops/sonarlint) — documentation drift, not implementation gaps
- Zero broken or stub features
- Agents are AI-only prompt docs with no automated test coverage (by design)

### D5: Docs/Markdown/YAML/JSON Audit — WARN (55)

| Finding | Severity | Count |
|---------|----------|-------|
| STALE | HIGH | 7 (README.md ×3, GEMINI.md ×3, install-manifest) |
| FORMAT_ISSUE | MEDIUM | 2 (CHANGELOG unreleased structure, GEMINI.md command syntax) |
| SCHEMA_ERROR | MEDIUM | 1 (governance skill references nonexistent CLI subcommands) |
| VERSION_MISMATCH | HIGH | 1 (install-manifest 0.1.0 vs manifest.yml 0.2.0) |
| ACCURATE | INFO | 24 |

**Key**: README.md is the public face of the project and contains v2 numbers throughout (47 skills, 6 agents, 53 slash commands). GEMINI.md mirrors this staleness. CLAUDE.md and AGENTS.md are accurate.

### D10: Canonical vs Template Drift — WARN (78)

- **66/69 shared files**: byte-level identical
- **2 drifted files**: `manifest.yml` (7 agents vs 6), `README.md` (34 skills vs 33) — canonical updated, template not synced
- **1 intentional drift**: `product-contract.md` (project-specific vs placeholder template)
- **Validator blind spot**: `mirror_sync.py` patterns don't cover `manifest.yml` or `README.md` (root-level files excluded from glob)
- **1 orphan**: `context/learnings.md` exists in template but not canonical

### D11: Decision Store & Audit Log Integrity — WARN (72)

| State File | Valid | Schema Match | Issues |
|------------|-------|-------------|--------|
| `decision-store.json` | PASS | PASS | snake_case key drift (`schema_version` vs `schemaVersion`) |
| `audit-log.ndjson` | PASS | PASS | Only `gate_result` events; 16 entries |
| `ownership-map.json` | PASS | PASS | Missing `session-checkpoint.json` rule |
| `install-manifest.json` | PASS | PARTIAL | Schema 1.1 vs model 1.2; 5 fields missing from file |
| `session-checkpoint.json` | PASS | N/A | No Pydantic model; all-null; outside governance perimeter |
| `sources.lock.json` | PASS | PASS | All checksums null; never verified |

- `state/service.py` facade is clean — no duplication with existing modules
- Decision lifecycle logic correctly aligned with model constraints

---

## Wave 2 — Code & Security

### D4: Python Code Value Audit — PASS (88)

| Verdict | Count | LOC |
|---------|-------|-----|
| KEEP | 101 | ~17,800 |
| SIMPLIFY | 5 | ~1,400 |
| MERGE | 3 | ~469 |
| DEPRECATE/REMOVE | 0 | 0 |

- **Zero dead or stub modules** — all 109 modules are actively imported and used
- **Highest-value action**: Merge tool-availability primitives from 3 modules into shared `tools_util.py` (~200 LOC elimination)
- **Decomposition assessment**: `doctor/checks/`, `policy/checks/`, `validator/categories/` are well-factored but backward-compat shims in parent modules remain (145 LOC total)
- **Largest modules**: `test_scope.py` (683L), `orchestrator.py` (664L), `setup.py` (536L), `workflows.py` (535L) — all justified by complexity

### D8: Security Enforcement Audit — WARN (75)

**Enforcement chain**: `git commit` → pre-commit hook (SHA-256 verified) → `ai-eng gate pre-commit` → branch protection + hook integrity + gitleaks + ruff → commit-msg validation → `git push` → pre-push gate → semgrep + pip-audit + pytest + ty

| Tool | Config | Integration | Status |
|------|--------|------------|--------|
| gitleaks | `.gitleaks.toml` | pre-commit (correct `protect --staged`) | PASS |
| semgrep | `.semgrep.yml` | pre-push | PASS |
| pip-audit | pyproject.toml | pre-push | PASS |
| ruff | pyproject.toml | pre-commit | PASS |
| ty | pyproject.toml | pre-push | PASS |
| sonar | sonar-project.properties | pre-push (advisory only) | WARN |

**Gaps**:
- GAP-1: Hook content stale vs current generator (source/activate vs PATH prepend)
- GAP-2: `cicd.py` generator uses weaker security commands (`detect --no-git` vs `detect --source .`)
- GAP-3: Sonar gate advisory-only — never blocks
- GAP-4: No SSRF semgrep rule despite OWASP mapping claiming coverage
- GAP-5: `.gitleaks.toml` allowlist includes entire `state/` directory

**Positive**: Claude Code deny rules correctly block `--no-verify`; fail-closed hook integrity; gitleaks pre-commit command correct in `stack_runner.py`

### D15: Cross-OS Reliability — WARN (72)

**CI Matrix**: Unit + integration + framework-smoke run on ubuntu/macos/windows. All other jobs (E2E, security, lint, coverage) are Linux-only.

| OS | Key Issues |
|----|-----------|
| macOS | `brew` assumed; no fallback if absent |
| Linux | `apt-get` hardcoded; fails on Alpine/RHEL/Arch |
| Windows | `.venv/bin/*` in allow-list (should be `.venv\Scripts\*`); no `.gitattributes` for line endings |

**Positives**: All `subprocess.run` uses list form (no `shell=True`); `pathlib` throughout; explicit UTF-8 encoding; `shutil.which` for tool detection; dual Bash/PowerShell hook generation

### D16: Installer/Doctor Operational Readiness — WARN (75)

- **Installer**: 16/16 bootstrap scenarios handled; create-only idempotent semantics; multi-provider template support
- **Doctor**: 8 check modules fully decomposed; `diagnose()` orchestrates correctly
- **Gaps**:
  - `check_platforms()` not wired into `diagnose()` — credential freshness unchecked in default doctor run
  - `--fix-tools` uses pip for gitleaks/semgrep (system tools, not on PyPI) — should use `ensure_tool()` from `installer/tools.py`
  - 3 test stubs with only `pass`

---

## Wave 3 — Contracts, E2E, Tests

### D2: Framework Contract Compliance — WARN (60)

| Status | Count |
|--------|-------|
| PASS | 20 (45%) |
| PARTIAL | 15 (34%) |
| FAIL | 4 (9%) |
| NOT_VERIFIABLE | 5 (11%) |

**4 FAIL clauses**:
1. **§1.4-b**: No empty-repo guided initialization wizard — installer defaults silently
2. **§2.1-e**: Commit format `spec-NNN: Task X.Y` not enforced — gate only checks length ≤72
3. **§2.2-c**: Branch naming convention not enforced — gate only blocks main/master
4. **§5.2-a**: Remote skills default OFF (`default_remote_enabled=False`) — contract says ON

**Key PARTIAL**: Agentic model clauses (§2) are largely documentation-enforced; 7 of 14 are NOT_VERIFIABLE or PARTIAL. Signature verification modeled but never implemented.

### D7: Command Contract E2E Validation — FAIL (40)

| Command | Verdict |
|---------|---------|
| `/ai:plan`, `/ai:execute` | WARN — AI-only, no Python backing, untestable |
| `/ai:commit` | FAIL — gitleaks wrong command; documentation gate absent |
| `/ai:commit --only` | FAIL — same issues |
| `/ai:pr` | FAIL — spec reset absent; existing PR upsert absent; doc gate absent |
| `/ai:pr --only` | WARN — interactive warning suppressed; continuation modes missing |

**6 violations identified**:
1. `workflows.py:225` uses `gitleaks detect --staged` (invalid in gitleaks 8.x) — **P0 security**
2. Documentation gate entirely absent from both commit and PR workflows
3. Spec reset step absent from `/pr` workflow
4. Existing PR check and body upsert absent
5. `/pr --only` auto-pushes silently instead of warning + offering continuation modes
6. Planning pipeline has zero automated test coverage (AI-only)

### D9: Ownership Boundary & Updater Safety — WARN (70)

- **Logic correct**: `OwnershipMap` correctly differentiates ALLOW/DENY/APPEND_ONLY; updater defaults to deny for unmatched paths; installer uses create-only semantics
- **On-disk stale**: `ownership-map.json` missing 3 paths (`.github/prompts/**`, `.github/agents/**`, `.claude/**`) present in `defaults.py`
- `session-checkpoint.json` absent from ownership-map despite being manifest-declared system-managed
- `remove_provider_templates()` can delete framework-managed files without ownership consultation

### D12: Test Coverage & Critical Gaps — WARN (68)

| Tier | Count | Actual % | Target % |
|------|-------|----------|----------|
| Unit | 978 | 69.1% | 50% |
| Integration | 417 | 29.4% | 45% |
| E2E | 21 | 1.5% | 5% |

- **6 test stubs** with only `pass` (no assertions)
- **Critical gaps**: `StateService` never instantiated directly; `validator/categories/` tested only through service facade; `policy/checks/commit_msg.py` no direct import in tests
- Test mapping integrity passes (all 122 source files covered by scope rules)

### D17: Weekly Maintenance/Compaction — WARN (65)

- All 8 maintenance commands implemented; 7/8 have test coverage
- **No automated schedule**: `manifest.yml` declares `cadence: weekly` but no CI cron workflow exists
- **Token budget breaches**: Session start with active spec exceeds ~500 token budget by 4×; `pr/SKILL.md` at 2,059 tokens (ceiling = 2,050)
- **Stale content**: README (v2 counts), GEMINI.md (v2 counts), 5 archived specs without `done.md`

---

## Wave 4 — Synthesis

### D6: Backlog + Delivery Architecture — WARN (72)

- **31 archived specs** + no active spec (clean idle state)
- **Schema maturity**: Specs 020+ follow schema consistently; older specs (001–019) show evolutionary drift
- **5 archived specs missing done.md** lifecycle markers (functionally handled by fallback detection)
- **9 specs with stale `status: in-progress`** despite having `done.md`
- **Status enum fragmented**: 5 distinct values in use (in-progress, done, completed, IN_PROGRESS, closed)
- **Pipeline classification is AI-only** — no Python implementation; classification accuracy untestable

### D13: Complexity Reduction Plan — WARN (70)

| Priority | Action | Savings |
|----------|--------|---------|
| P1 | Remove `gates.py` `__getattr__` backward-compat (migrate tests first) | ~65 LOC |
| P2 | Extract `doctor/models.py` — break circular import chain | ~30 LOC |
| P3 | Remove `doctor/service.py` wrapper functions | ~80 LOC |
| P4 | Merge tool-availability code into shared module | ~30 LOC |
| P5 | Rename `CheckStatus` in `validator/_shared.py` to `IntegrityStatus` | clarity |
| P6 | Extract `check_platforms` into per-platform helper | ~60 LOC, CC 12→4 |
| P7 | Extract `TEST_SCOPE_RULES` data to separate file | ~270 LOC moved |

- **Cyclomatic complexity hotspots**: `_compute_scope_internal` (CC~14), `check_platforms` (CC~12), `_parse_counter` (CC~8)
- **Circular deps**: 3 identified (all mitigated via deferred imports); root cause is shared types in orchestrator modules

### D14: Release Readiness Gate — NO-GO (45)

**Verdict: NO-GO** — 1 P0 blocker

**P0 (blocks release)**:
1. `workflows.py:225` — gitleaks `detect --staged` is invalid; must be `protect --staged`

**P1 (fix shortly after)**:
1. `install-manifest.json` version 0.1.0 → 0.2.0 to match manifest.yml
2. `install-manifest.json` schema 1.1 → 1.2
3. Documentation gate absent from `/pr` skill
4. README.md stale v2 counts
5. Undeclared CLI commands (doc drift)
6. `ownership-map.json` missing 3 paths

**Minimum closure path**: Fix gitleaks command (1 line change + test), then all gates pass for release prep.

### D18: Architecture Drift — WARN (75)

- **Declared-vs-actual**: All declared components exist in code. No declared component missing.
- **CLI surface drift**: 11 undeclared subcommands (implemented but not in governance docs)
- **Module governance gap**: `commands/workflows.py` implements key `/ai:commit` and `/ai:pr` flows but has no governance entry in manifest.yml or framework-contract.md
- **3 circular dependencies** (all mitigated via deferred imports): root cause is `GateCheckResult`/`GateResult` defined in orchestrator rather than shared types module
- **Clean boundaries**: `git/`, `lib/`, `vcs/`, `validator/`, `version/` are well-isolated leaf packages

---

## Appendix A: File Coverage Matrix

Every `.py` file in `src/ai_engineering/` was analyzed in at least one dimension:
- **D3**: All CLI commands, agents, skills (surface inventory)
- **D4**: All 122 `.py` files (module verdict)
- **D7**: `commands/workflows.py`, `vcs/github.py`, `vcs/protocol.py` (E2E contract)
- **D8**: `hooks/manager.py`, `policy/gates.py`, `policy/checks/*` (security chain)
- **D12**: All 79 test files mapped to source modules
- **D18**: Full import graph across all 28 packages

Every governance file in `.ai-engineering/` was referenced:
- **D5**: All state JSON/NDJSON, manifest.yml, README, contract, specs
- **D9**: `ownership-map.json` + all tiers
- **D10**: Full canonical vs template comparison (69 paired files)
- **D11**: All 6 state files individually validated

## Appendix B: Uncommitted Changes Impact

```
M .ai-engineering/state/audit-log.ndjson   (gate event entries from today)
```

Single modified tracked file — audit log append-only. No impact on release readiness beyond being uncommitted.

## Appendix C: Prioritized Action Plan

### P0 — Must Fix Before Release

| ID | Action | File | Est. Effort |
|----|--------|------|-------------|
| P0-1 | Fix gitleaks command: `detect --staged` → `protect --staged` | `src/ai_engineering/commands/workflows.py:225` | 5 min |

### P1 — Fix Shortly After Release

| ID | Action | File(s) | Est. Effort |
|----|--------|---------|-------------|
| P1-1 | Update `install-manifest.json` frameworkVersion 0.1.0 → 0.2.0 | `.ai-engineering/state/install-manifest.json` | 5 min |
| P1-2 | Update `install-manifest.json` schemaVersion 1.1 → 1.2 + add missing fields | `.ai-engineering/state/install-manifest.json` | 15 min |
| P1-3 | Update README.md with v3 counts (34 skills, 7 agents, 37 commands) | `README.md` | 30 min |
| P1-4 | Add documentation gate to commit/PR workflows | `commands/workflows.py` | 2 hr |
| P1-5 | Regenerate `ownership-map.json` from updated defaults | `state/defaults.py` + `state/ownership-map.json` | 15 min |
| P1-6 | Sync template `manifest.yml` and `README.md` | `src/ai_engineering/templates/.ai-engineering/` | 15 min |

### P2 — Planned Backlog

| ID | Action | Origin |
|----|--------|--------|
| P2-1 | Merge tool-availability primitives into shared module | D4, D13 |
| P2-2 | Remove `gates.py` `__getattr__` + doctor/service.py wrappers | D4, D13 |
| P2-3 | Extract `doctor/models.py` to break circular imports | D13, D18 |
| P2-4 | Add E2E tests for commit/PR workflows (target 5%) | D12 |
| P2-5 | Implement 6 test stubs with actual assertions | D12, D16 |
| P2-6 | Add `.gitattributes` for cross-OS line endings | D15 |
| P2-7 | Fix Windows venv paths in `.claude/settings.json` template | D15 |
| P2-8 | Wire `check_platforms()` into `diagnose()` | D16 |
| P2-9 | Add CI cron for `ai-eng maintenance all` | D17 |
| P2-10 | Add SSRF semgrep rule | D8 |
| P2-11 | Update GEMINI.md to v3 | D5 |
| P2-12 | Add `mirror_sync.py` coverage for root-level files | D10 |
| P2-13 | Implement spec status close-time enum enforcement | D6 |
