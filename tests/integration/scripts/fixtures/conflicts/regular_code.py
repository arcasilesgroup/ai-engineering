"""Plain Python module — classifier must mark this as code/manual."""

from __future__ import annotations


def greet(name: str) -> str:
    """Return a greeting; trivial body keeps the fixture stable."""
    return f"hello, {name}"
