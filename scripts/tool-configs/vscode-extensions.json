{
  // ==========================================================================
  // AI Engineering Framework - Recommended VS Code Extensions
  // ==========================================================================
  // VS Code will prompt to install these when the project is opened.
  // ==========================================================================
  "recommendations": [
    // Capa 1: SonarLint (Connected Mode for server-synced rules)
    "SonarSource.sonarlint-vscode",

    // Capa 2: Stack-specific linters
    "dbaeumer.vscode-eslint",           // TypeScript/JavaScript linting
    "esbenp.prettier-vscode",           // TypeScript/JavaScript formatting
    "charliermarsh.ruff",               // Python linting + formatting
    "hashicorp.terraform",              // Terraform language support + formatting
    "ms-dotnettools.csdevkit",          // C# Dev Kit (Roslyn analyzers)

    // Cross-IDE formatting baseline
    "editorconfig.editorconfig"
  ]
}
