# GitHub Copilot Instructions

> Generated by ai-engineering v{{version}} on {{timestamp}}
> Stacks: {{join stacks ", "}} | Level: {{level}}

## Critical Rules

- Never use `git push --force` or `git push -f`
- Never commit directly to: {{join protectedBranches ", "}}
- Never commit secrets or sensitive files
- Never use `--no-verify` on git commands
- Always use conventional commits
- Always verify changes with tests

## Branch Model

- Default: `{{defaultBranch}}` | Integration: `{{developBranch}}`
- Features: `feature/*` or `dev/*`
- Fixes: `fix/*` | Hotfixes: `hotfix/*`

{{{standards}}}

## Security

{{{security}}}

## Git

{{{git}}}

## Testing

{{{testing}}}
