# Review request

## Original task

User (verbatim): "revisa la implementación ahora si se ha hecho bien todo, puedes lanzar un agente nuevo de deepseek para valiar esta parte y hacer este audit. Estamos ante una review de código y vamos a analizar la calidad y la seguridad"

Round-15 UX milestone context: prior commit 97ab8701 "cli-ux-15 re-verified post-P0-fixes". The working tree on top of HEAD contains follow-up fixes.

## Stated constraints and acceptance criteria

- Tests must pass: bun test (currently 70/70 green on this tree)
- bun run build must compile dist/ai-eng
- Project conventions in AGENTS.md (no noqa/@ts-ignore, no secrets, no machine paths, KISS)

## Stack

- Bun 1.4.0 / TypeScript, zero-runtime deps: @bomb.sh/args, @bomb.sh/tab, @clack/prompts 1.7.0
- Tests: bun test (70 tests). Lint: oxlint. Typecheck: tsgolint
- Binary payload: src/assets.ts embeds skills/ and templates/ via scripts/gen-assets.ts

## Scope
Working tree changes vs HEAD (97ab8701), branch v2: 27 modified files + 1 rename (src/plant.ts -> src/install.ts) + 2 new source files + 2 new test files. Excluded from review: docs/*.html, skills/*/SKILL.md and references (prose), templates/*.tpl (3-line changes), src/assets.ts (generated), .ai-engineering/arch.rules.json (config).

## Files changed (review these)
- src/install.ts — new; 150 lines; install/recovery entry shared by init and update paths
- src/wrap/paint.ts — new; 182 lines; terminal rendering of chain/test status
- src/wrap/index.ts — modified; 85 lines changed; uses paint.ts
- src/commands/uninstall.ts — modified; 139/64 lines changed; largest modified file
- src/commands/update.ts — modified; 87/49
- src/commands/init.ts — modified; 65/35
- src/ui.ts — modified; 81/12; terminal UI helpers
- src/commands/doctor.ts — modified; 29/20
- src/commands/config.ts — modified; 22/14
- src/surfaces/adapters.ts — modified; 20/11
- src/env.ts — modified; 20 added lines
- src/cli.ts — modified; dispatch
- src/branding.ts, src/commands/init-shared.ts, src/commands/upgrade.ts, src/guards/no-verify.ts, src/guards/self-protect.ts, src/spec/index.ts, scripts/proof-cli-ux.sh — smaller edits
- tests/wrap.test.ts — new; 179 lines
- tests/adversarial/init-recovery.test.ts — new; 139 lines

## Diffs
Pasted in the two files next to this packet:
- packet diffs part 1: /Users/soydachi/repos/ai-engineering/.claude/reviews/packet-20260903-114139-diffs1.md
- packet diffs part 2: /Users/soydachi/repos/ai-engineering/.claude/reviews/packet-20260903-114139-diffs2.md
New untracked source files (read them directly): src/install.ts, src/wrap/paint.ts, tests/wrap.test.ts, tests/adversarial/init-recovery.test.ts

## Your task
Review this change for two things:

1. **Bugs** — logic errors, off-by-ones, wrong operators, unhandled null/empty/zero cases, broken error paths, race conditions, resource leaks, incorrect API usage, changes that break existing callers, and any Bun/Node API used in a way the installed Bun 1.4.0 does not support.
2. **Security** — command injection, path traversal, unsafe file writes outside the repo, symlink attacks, secrets handling, unsafe process spawning, hook bypass possibilities.

Read the surrounding code before judging. The listed files are the change, but the repo is open to you — check callers, types, tests, and config to see how this code is actually used.

Skip style, naming, formatting, and refactors that do not change behavior.

For every finding, give:
- `file:line`
- what is wrong, in one sentence
- **a concrete failure case: specific inputs or state, and the wrong output or crash that results**
- severity: high (wrong results, data loss, crash on a normal path) / medium (breaks on an edge case) / low (works, but fragile)

If a finding cannot be expressed as a concrete failure case, do not report it.
That test is the whole difference between a review and a list of suspicions.

If you find nothing, say so explicitly and say what you checked. Do not pad.

## Output
Print your review to stdout as your final message. You have no write tools; do not try to save files.
