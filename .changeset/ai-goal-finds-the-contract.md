---
"ai-engineering": patch
---

ai-goal finds the milestone on its own: Step 0 reads the spec slot from `ai-eng doctor`, then spec.html's context chain, plan.html, the wayfinder map and the `[budget]` section, so a fresh session is launched as `/ai-goal` with no briefing prompt.
