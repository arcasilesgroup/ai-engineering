# Requirement-by-requirement audit, 2026-08-16

The second full pass over the two research documents this repository is built from, measured
at HEAD `95b88c9f` under the same rule as the first:

> **A requirement is PROVEN only if something executed and the decisive output can be
> pasted.** Reading code is not proof. A file existing is not proof that the file does what
> the requirement asks. Where nothing could be executed, the verdict says so.

The documents are `.ai/reports/process-optimization-research/index.html` (26 commitments,
`PO-01`..`PO-26`) and `.ai/reports/evolution-proposal/index.html` (385 requirements,
`EP-001`..`EP-385`).

Five read-only auditors ran the closing table of one specification each — 011, 012, 013,
014, and 015 with 016 — executing the test that would fail if the requirement were unmet, or
citing the file and line that decides it. Their verdicts replace the previous audit's for
the 253 requirements a specification claims to close. The other 132 keep the verdict of
2026-08-15, because nothing in this session touched them.

Where two auditors reached the same requirement and disagreed, the **more conservative
verdict stands** — `EP-283`, `EP-300`, `EP-301` and `EP-273` are recorded INCOMPLETE for
that reason. A proof that another reader can contradict is not a proof.

## The answer

**`evolution-proposal` is not implemented, and it is half proven.** This section is the
first pass of 2026-08-16; a second pass the same day is recorded below it and supersedes
these counts. 192 of 385 requirements
are proven by execution or by a measured absence, against 120 four weeks of work ago. Nothing
in 385 is contradicted: where the tree acts, it acts as the document asks; what is missing is
missing whole.

The number worth carrying is not 49.9%. It is this: **fourteen of the twenty requirements
this repository recorded as FAILED on 2026-08-15 are closed, six are not, and six proofs the
previous audit granted have been withdrawn.** An audit that only ever moves upward is a
press release.

## Verdicts by range

| range | PROVEN | INCOMPLETE | NO-EVIDENCE | CONTRADICTED | total |
|---|---:|---:|---:|---:|---:|
| EP-001..EP-020 | 10 | 7 | 3 | 0 | 20 |
| EP-021..EP-102 | 24 | 41 | 17 | 0 | 82 |
| EP-103..EP-218 | 61 | 45 | 10 | 0 | 116 |
| EP-219..EP-320 | 59 | 21 | 22 | 0 | 102 |
| EP-321..EP-385 | 38 | 25 | 2 | 0 | 65 |
| **total** | **192** | **139** | **54** | **0** | **385** |

Against 2026-08-15: 49 moved INCOMPLETE to PROVEN, 29 moved NO-EVIDENCE to PROVEN, 28 moved
NO-EVIDENCE to INCOMPLETE, 20 moved INCOMPLETE to NO-EVIDENCE, and **6 moved PROVEN to
INCOMPLETE**.

## The twenty that were FAILED

**Fourteen are closed.** Each was re-run, not re-read.

| was | now | what closed it |
|---|---|---|
| PO-09 | closed | `specs/010` reads `status: draft` again; the contradiction between the file and its own plan is gone. The transition still waits on exact-HEAD receipts, which is the plan working. |
| PO-24 | closed | `docs/adr/0009` records both approved digests and `tests/test_record.py::test_the_approval_digests_in_the_plan_are_read_by_something` reads them against the files. |
| PO-15, PO-20 | closed | `git grep -cE "noqa\|ts-ignore\|nosec" -- src hooks tests` → only `tests/stats.py`, which counts them. |
| EP-033, EP-040 | closed | `grep -c merge_group .github/workflows/check.yml` → 1, and `test_the_combined_result_gates_and_not_only_each_branch_alone` passes. |
| EP-286 | closed | `python tests/adversarial/run.py` → 21 of 21, with a clean control for each of ten attacked guards. |
| EP-011, EP-144 | closed | `ai-eng audit verify` → FAIL and `doctor` assertion 6 → FAIL, on the same file, naming the same link. The two readers agreed on green and disagreed on red; now they agree. |
| EP-284 | closed | the bound reads 50 ms, measured as a floor, and the register carries `guard_p95_ms` as `no_instrument` rather than a median wearing the name p95. |
| EP-204 | closed | `doctor.surfaces_alive` compares ids in a set instead of testing membership against a tuple, so the INERT path can be reached at all. |
| EP-287 | closed | `hooks/session.py` reads what `send_tail` answers, records the rejection and says it out loud. |
| EP-222 | closed | `ai-eng --json update --dry-run` → `WOULD_CHANGE`. A dry run no longer reports a pass for work that did not happen. |
| EP-300 | closed | `test_the_surface_list_has_one_home_and_the_rest_derive_from_it` passes; `surface.py` derives from `wiring.table()`. |

**Six are open**, and each names who can close it.

- **EP-029** — `ai-eng report` offers `digest`, `issue` and `surfaces`. There is still no
  routing evaluation per capability: no approver, no format, no location, no command. Ours.
- **EP-060** — the adversarial suite has a control per attacked guard; the seven recipes
  `just check` runs still have none, and no reason is recorded for the gap. Ours.
- **EP-232** — the envelope's check objects carry `cure` now and still carry no `evidence`.
  Ours, and small.
- **EP-049** — the release publishes a wheel and provenance and no SBOM. Needs a release,
  which is the operator's.
- **EP-061** — no sequential-versus-parallel benchmark exists. Needs a pilot, which is the
  operator's.
- **PO-16** — one primary home per commit did not hold, and the operator granted a written
  exception for the commits that move a count or a ceiling another check forces them to
  move. Recorded rather than closed.

## The six proofs this audit withdrew

The previous audit granted these and the re-run would not.

- **EP-077, EP-079, EP-090** — each was credited to a mechanism that exists beside the
  requirement rather than under it: a file-home check for an adapter claim, a readable
  `audit verify` for an envelope claim, two pinned dependencies for a schema claim. There is
  still no envelope schema in `policy/` and `spec list --json` emits a `schema_version` with
  no `schema`.
- **EP-285** — `FLOOR = 89` exists in the justfile and no whole-tree mutation score does.
  The lane that would produce one has never completed on this branch: it hits its
  30-minute cap mid-run. A floor nothing has ever measured against is a number, not a gate.
- **EP-287** — closed above as a FAILED item and withdrawn as a P5 indicator, which is not a
  contradiction: the rule is now read and recorded, and no command publishes the number.
- **EP-308** — a closed fifteen-capability list is not a check that a sixteenth capability
  cannot be declared without governance.

## The process-optimization commitments

The 2026-08-15 audit recorded 9 PROVEN, 5 FAILED, 12 other. Four of the five FAILED are
closed above; PO-16 carries its written exception. What has to be said beside that:

**this session's work ran outside the protocol it audits.** The block cadence — a block
boundary, an independent reviewer per block, one bounded re-review — governs implementation
blocks. What ran here after Block D was continuous-integration repair: fifteen commits, each
gated by `just check` and pushed, with no block boundary and no independent reviewer. That is
not a violation of a rule that covers it; it is work the protocol has no rule for, which is
the fourth gap this repository has now recorded in it. The others are a repair that
introduces a defect, a re-review that returns REJECT, and a checkpoint that turns a shipped
test red.

## What the audit found that nobody had asked for

Seven defects, none of them in scope, all of them the same shape — a control that reads
stronger than it is:

1. `actionlint` and `zizmor` ran after `just check` in the same job, so on a branch that had
   never been green they had never executed once. Their first run found a branch name pasted
   into a shell script.
2. `ai-eng doctor --ci` cannot exit 0 on any commit, and the gate took its exit code as a
   verdict.
3. `ai-eng audit verify` ran before anything had been sealed into the chain, so it could
   neither pass nor verify anything.
4. The fenced-block recogniser in the acceptance register required a bare newline, so a spec
   file written on Windows read as holding no acceptances — a PASS over an empty register.
5. `decide --accept` put command-line text into a glob pattern; `..` is a legal glob segment.
6. `authority_role` from the Intent was interpolated into the MADR header between bare
   quotes, so an edited Intent could forge `status` and `supersedes`.
7. The Codex hook entry used three key names Codex CLI does not define, so that guard could
   never have deserialised.

## Measured again the same day, at `be9ea04c`

Twenty commits after the pass above, four read-only auditors re-ran the 168 requirements
that were still unproven and a fifth re-verified the 28 this session claimed to close. The
counts moved, and so did three verdicts nobody had seen before in this record.

| verdict | first pass | second pass |
|---|---:|---:|
| PROVEN | 192 | **236** |
| INCOMPLETE | 139 | 124 |
| NO-EVIDENCE | 54 | 22 |
| **CONTRADICTED** | 0 | **3** |
| total | 385 | 385 |

The 149 that were already PROVEN and are not named below keep the verdict and the evidence
of the first pass; only the 196 that were not proven were re-measured, plus the 28 verified.

### The three CONTRADICTED, and they were between two of our own files

`policy/capabilities.toml` declares fifteen capabilities. `.agents/skills/` holds twelve.
The three declared and absent — `ai-test`, `ai-verify`, `ai-animation` — are exactly the
three that `test_the_three_absorption_candidates_did_not_ship_and_their_work_has_a_home`
asserts must not exist. So the manifest said this product has a capability while a passing
gate said its skill must not be there, and each side had a green test of its own.

`EP-333`, `EP-382` and `EP-383` ask for those three to be recovered first-class. The tree
does the opposite, deliberately and with a recorded reason, which is what makes them
contradictions rather than gaps. Neither file moved to fix it: a capability with no skill is
legal now only while the absorption map names where its work went, and a skill nobody
declared is refused from the other side. What was missing was anything reading both.

### The 20 that moved to PROVEN, by execution

`EP-007`, `EP-164`, `EP-172`, `EP-189`, `EP-287`, `EP-301`, `EP-308`, `EP-316`, `EP-334`,
`EP-345` and `EP-367` closed on work that landed after the first pass. `EP-090`, `EP-079`,
`EP-081`, `EP-184`, `EP-028`, `EP-248`, `EP-115`, `EP-116`, `EP-127`, `EP-128`, `EP-041`,
`EP-261`, `EP-066`, `EP-076`, `EP-084`, `EP-207`, `EP-014`, `EP-017`, `EP-205` and `EP-212`
were verified independently, each by a command the verifier ran rather than by the commit
message that claimed it.

### Five overclaims the verifier found, kept visible

Two were fixed: three of thirteen accessibility items were pinned and the other ten could be
deleted with the suite green; and every router fixture planted a fabricated surface, so
nothing showed `init` reaching the generator. Three stand and are not claimed as closed —
the shipped adapter's translations are four identity mappings, so the read is real and
currently inert; `claim-order` answers SKIPPED in the gate because a one-writer repository
holds no claims, so `dag.order` still never executes there; and nothing emits a seven-field
security finding, which `D-014-01` already says.

### Seven verdicts in the first pass that were wrong when written

`EP-003`, `EP-138`, `EP-180`, `EP-330`, `EP-338`, `EP-343` and `EP-157` were graded against
files the audit said were absent and that already existed at its own commit. They are
corrections, not movement, and they are counted as corrections above.

### The 26 process commitments, re-measured

The first pass recorded 9 PROVEN, 5 FAILED and 12 other. Four of the five failures are
closed: `PO-24`'s approval digests are read by `tests/test_record.py`, and `PO-15`/`PO-20`
were re-measured rather than assumed — which found an inline linter exemption written during
this session, in this repository, and removed it.

`PO-09` stays open. `specs/010` reads `status: draft`, which is correct: the plan reserves
that transition until a candidate proves its own exact-HEAD CI receipts, and two required
lanes are red for reasons the branch cannot resolve from inside.

`PO-16` stays open with its written exception. The widest commits of this session touch
eleven files, and the exception the owner granted covers a commit moving a count or a
ceiling another check forces it to move — not eleven files.

And the honest one: **`PO-01`, `PO-06` and `PO-13` did not govern this session.** The block
cadence — a block boundary, an independent reviewer per block, one bounded re-review —
covers implementation blocks. What ran after Block D was continuous-integration repair and
requirement closure: roughly twenty commits, each gated by `just check`, with no block
boundary. Independent review happened twice and neither time as the protocol describes: a
verifier re-ran twenty-eight closures and found five overclaims, and four auditors re-ran
168 requirements and found three contradictions. Both found real defects. Neither is the
rule, and the protocol still has no rule for this shape of work — the fifth gap now recorded
in it.

### What no local work can move

Twenty-five requirements are blocked by construction rather than by effort: a published
release (`EP-047`, `EP-049`, `EP-052`, `EP-094`, `EP-151`, `EP-211`, `EP-280`), a merge
queue — checked by API on this repository and **not configured** (`EP-033`, `EP-038`,
`EP-039`, `EP-086`, `EP-185`, `EP-329`), a network remote or a second machine (`EP-030`,
`EP-031`, `EP-036`, `EP-182`, `EP-186`, `EP-183`), an external organisation (`EP-058`), a
real pilot with a sequential baseline (`EP-054`, `EP-059`, `EP-061`), and the editors
themselves invoking a denial (`EP-018`, `EP-209`, `EP-213`, `EP-214`, `EP-215`).

`evolution-proposal` is not implemented. It is 61% proven, nothing in it is contradicted by
accident, and the three that are contradicted are contradicted on purpose with the decision
written down.

## Appendix: every requirement

### The 192 PROVEN, as ranges

Each was executed by the auditor who recorded it, or proven by a measured absence.

EP-002, EP-004–EP-006, EP-009, EP-011–EP-013, EP-019–EP-021, EP-023–EP-025, EP-027, EP-032,
EP-034–EP-035, EP-045, EP-050, EP-067, EP-069–EP-072, EP-088–EP-089, EP-092, EP-097–EP-112,
EP-118, EP-120–EP-121, EP-123–EP-126, EP-129–EP-134, EP-136, EP-139, EP-141–EP-150,
EP-152–EP-156, EP-159, EP-174, EP-177, EP-181, EP-187, EP-190–EP-198, EP-200, EP-202–EP-204,
EP-208, EP-217–EP-238, EP-240, EP-242, EP-247, EP-249–EP-251, EP-254–EP-255, EP-257–EP-258,
EP-265–EP-272, EP-274–EP-277, EP-279, EP-286, EP-288, EP-290, EP-294–EP-296, EP-299, EP-304,
EP-306–EP-307, EP-309–EP-310, EP-312, EP-315, EP-317–EP-318, EP-321, EP-323, EP-327, EP-331,
EP-335–EP-337, EP-339–EP-342, EP-347–EP-351, EP-353, EP-355–EP-356, EP-358–EP-360,
EP-362–EP-363, EP-365, EP-368–EP-369, EP-371–EP-372, EP-374–EP-381, EP-384


### The 139 INCOMPLETE

| id | why it is not proven |
|---|---|
| EP-001 | madr.py:567 requires a resolving `supersedes` edge (test_madr.py::test_madr_validator_fails_closed_on_invalid_graph_and_transition); no check… |
| EP-003 | docs/adr/0005 supersedes 0004 and validates (test_madr.py::test_intent_supersession_madr_is_complete), but 0005 is `proposed` and 0004/spec 00… |
| EP-007 | test_cli_migration.py::test_exception_is_hard_rename_without_plan_alias and ::test_report_is_hard_rename_and_bare_report_refuses pass; report.… |
| EP-008 | Relations resolve: `madr.validate(ROOT)=="PASS"` (test_madr.py:441). Every docs/adr MADR is `proposed` and spec 010 is `draft` — nothing appro… |
| EP-010 | One code path writes the frontmatter (test_cli_migration.py::test_spec_success_has_exact_facts_and_json_and_pending_is_not_canonical); nothing… |
| EP-015 | links half proven; agents/openai.yaml does not exist |
| EP-016 | the receipt carries id, tool_version and digests; no adapter_version, no deny-protocol field |
| EP-022 | AA is stated as the floor; AAA has no pass condition and no gate blocks on any level |
| EP-028 | the rule is stated; the enumerated definition of done is not landed |
| EP-030 | the sixth guard and two subcommands landed; real concurrency against origin is unproven, every fixture uses git's file transport |
| EP-031 | the compare-and-swap lease exists; one remote branch and one worktree per item does not |
| EP-033 | merge_group is present and the queue itself is platform configuration nothing in the tree can read |
| EP-036 | proven against a bare repository over the file transport; the fixture's own docstring says it cannot prove the network one |
| EP-037 | the guard half is proven; the CI step exists and has never executed |
| EP-038 | two disjoint claims pass; nothing integrates them through a queue |
| EP-039 | proven as a local checkpoint verdict, not at a real merge gate |
| EP-040 | one coordination case and one control are in the adversarial suite; the race, stale-SHA and overlap fixtures live in pytest |
| EP-041 | the skill exists with method and baseline; no findings schema and no serialised findings |
| EP-044 | file-line findings and the challenge are stated; no executed check reads either |
| EP-046 | neither cross-check is recorded anywhere in the tree |
| EP-048 | the configuration switch is deleted; the command's first token still leaves in clear and no retention is stated |
| EP-049 | needs a published release tag and network the runner does not have |
| EP-051 | a versioned tamper fixture exists for the rules file; none for a scanner binary, an SBOM or a wheel |
| EP-052 | as EP-049, and there is no SBOM to validate |
| EP-054 | no with-answers and without-answers fixture pair exists |
| EP-057 | the prohibition half is a row; the threshold half does not exist |
| EP-058 | requires a named external organisation; none exists in the tree |
| EP-060 | a control per attacked guard; the seven just-check recipes still have none and no reason is recorded |
| EP-065 | the one-home half passes; one evidence per promise has nothing behind it |
| EP-066 | the mission's verbs are unchanged and no check reads them |
| EP-074 | routed to a draft specification; no ai-test or ai-verify skill exists |
| EP-075 | routed to two draft specifications; one adapter of eight |
| EP-076 | the constitution sections are present; the verbs half is EP-066, unmet |
| EP-077 | one adapter exists against eight declared surfaces |
| EP-078 | the admission checks exist; fifteen capabilities are declared and nothing enforces them |
| EP-079 | the human half prints; the JSON half is EP-090, unmet |
| EP-080 | the verb exists with a separate submit flag; routed to a draft specification |
| EP-081 | policy/adapters/ holds the translations and no production code reads them; chain.py still normalises from its own hardcoded aliases |
| EP-082 | the lane routes through the three-state contract; routed to a draft specification |
| EP-083 | the eight gates stand; the evals half has no runner |
| EP-084 | the event body carries no surface, no adapter and no undetermined |
| EP-085 | routed to a draft specification |
| EP-086 | merge_group is present; the rest is unverified |
| EP-090 | no envelope schema in policy/; spec list --json emits a schema_version with no schema |
| EP-091 | no ai-animation skill; the motion lens is a reference file |
| EP-094 | the release workflow names the SBOM as another wave's work and no SBOM step exists |
| EP-095 | the spec template has six headings and no audience or value section |
| EP-096 | the three modules exist; routed to a draft specification |
| EP-113 | ai-debug/SKILL.md:20,28-36 states cause-at-file:line and red-first; nothing executes the skill or checks its output |
| EP-114 | ai-debug/SKILL.md:8-10,40-44 is prose; contract.py:366 checks only that a "Not for" clause exists |
| EP-115 | ai-test absorbed; the risk to BDD to unit/integration/e2e/negative matrix appears nowhere |
| EP-116 | the absorption home exists; its three refusals are in no file |
| EP-119 | imposes-no-style is prose in the skill; no gate claims it |
| EP-127 | ai-verify absorbed into a ten-line reference; the spec-criterion-command-version-exit-digest manifest is not in it |
| EP-128 | runs-allowlists-without---fix matches no file |
| EP-135 | No five-phase grouping in README.md, init.py or surfaces/; description carries trigger + "Not for" (contract.py:366); review lenses stay in-skill |
| EP-137 | Six fields declared for all 15 caps (`test_capabilities_toml_declares_exactly_fifteen_capabilities`); capability.py:384 enforces nothing, no c… |
| EP-138 | capability.py:384 returns INCOMPLETE/ENFORCEMENT_UNAVAILABLE for every declared action; doctor.py:874 grades an UNPROVEN surface WARN, not INC… |
| EP-151 | `release.yml:91` id-token, `:102` attest-build-provenance, `:105` pypi-publish; SBOM only prose (`spec.md:379`) (untestable as stated) |
| EP-157 | `install-matrix.yml:412` runs a packaged guard from the wheel and requires exit 2 from no_verify_guard; nothing records version or date |
| EP-158 | `contract.py:366` requires a "Not for X" routing clause (`test_every_skill_meets_the_contract`); responsibility unmeasured (untestable as stated) |
| EP-161 | `contract.py:341` turns an over-80-line SKILL.md into an error, the only coded part of rule 12; no repeatability threshold (untestable as stated) |
| EP-162 | deny proof ids declared in `capabilities.toml`, asserted non-empty at `test_capabilities.py:283`; no corpus exists (untestable as stated) |
| EP-163 | `contract.py:19,379-382` jargon ban executes via `test_every_skill_meets_the_contract`; no readability check (untestable as stated) |
| EP-164 | `test_capabilities_toml_declares_exactly_fifteen_capabilities` pins ai-spec to one mode; nothing forbids a second handler or council elsewhere |
| EP-165 | human_gate declared per mode, asserted at `test_capabilities.py:278-280`; `capability.py:384` preflight still returns ENFORCEMENT_UNAVAILABLE |
| EP-166 | `.agents/skills/ai-spec/SKILL.md:25-27,38-40` states research-before-asking; nothing executes it (untestable as stated) |
| EP-167 | `spec.py:62-89` template has problem/2 options/decision; self-challenge, assumptions and BDD only in `ai-spec/SKILL.md:32-37` prose |
| EP-168 | `ai-spec/SKILL.md:38-40` only; no code detects an interactive channel or ranks a human answer over inference |
| EP-169 | `ai-spec/SKILL.md:50-52` states the least-scope reversible default; no ranking rule in code (untestable as stated) |
| EP-170 | `ai-spec/SKILL.md:54-59` names the four escalation triggers; nothing detects them (untestable as stated) |
| EP-171 | `ai-spec/SKILL.md:58-59` "More reviewers do not change this boundary"; no council code, no gap metric (untestable as stated) |
| EP-172 | accept/exception gates execute (`test_an_agent_cannot_grant_itself_a_bypass`); `ai-eng report issue` exits 1 INCOMPLETE — no publication gate |
| EP-173 | no envelope, voting or roles in the tree; deferral is prose at `spec.md:399-400`; only the closed capability-id list would notice one arriving |
| EP-175 | `test_outcomes.py:170` UNKNOWN normalises to INCOMPLETE and validators execute; agent self-challenge is `ai-spec/SKILL.md:32-33` prose |
| EP-176 | self-grant refused (`test_an_agent_cannot_grant_itself_a_bypass`); publication/secrets/deploy ungated — `report.py:66` returns INCOMPLETE |
| EP-178 | `ai-spec/SKILL.md:54-57` returns INCOMPLETE for such decisions; no code detects the triggers (untestable as stated) |
| EP-179 | checkpoint freshness exists; snapshot is named as a contract term nowhere |
| EP-182 | as EP-031 |
| EP-184 | claims, the gate and evidence are in CI; the DAG is checked nowhere and has no caller outside its own test |
| EP-185 | as EP-033, and no UNPROVEN report for a combined result exists |
| EP-186 | only fetch-before-claim is automated; approval, PR-ready, queue and merge have no code |
| EP-188 | the write guard and the remote re-derivation are proven; no CI check for duplicate claims exists |
| EP-189 | two receipts are produced and tested; secrets is delegated to a git hook and nothing writes the third receipt |
| EP-199 | install-matrix.yml:363-437 runs install→deny→doctor, but load and invoke are never separate executed states (P1, spec.md:274). |
| EP-201 | T3=instructions proven (test_mut_wiring.py:562); doctor.OPEN (doctor.py:72-73 block) says git is walked past by --no-verify, so "floor is git"… |
| EP-206 | one of eight surfaces has an executed denial; no per-surface pinned version exists |
| EP-209 | as EP-206 |
| EP-210 | as EP-206 |
| EP-211 | Wheel denial runs on push/PR only (install-matrix.yml:7-10), not per release (release.yml:9 tags); latency test is median-of-5, not p95 (test_… |
| EP-214 | as EP-206 |
| EP-215 | as EP-206 |
| EP-216 | Wired-by-name and no fabricated detection proven (test_mut_init.py:325, test_contracts.py:113); dispatcher matcher at chain.py:152-154; no pin… |
| EP-248 | twelve of the enumerated items have no home; the frontend lens covers keyboard, focus, states, contrast and reflow |
| EP-253 | imagery is opt-in; classification, residency, retention and consent are in no file |
| EP-256 | OCR-as-data is present; an-image-proves-nothing is in no file |
| EP-261 | per-finding states are stated as prose; nothing emits a findings record |
| EP-263 | still one trivy lane over the whole tree; no per-stack manifest |
| EP-264 | the lens exists as a checklist file; nothing binds it to a diff |
| EP-273 | no autonomous-submit path and no versioned organisational policy exist; only human-confirmed submit is proven |
| EP-278 | guard_p95_ms carries no_instrument; the check measures a floor, not a p95 |
| EP-282 | as EP-046 |
| EP-283 | surface.py enforces a 31-day ceiling where the register states seven days, and no surface receipt exists to have an age |
| EP-284 | no_instrument; the check measures the minimum of twenty pairs, which is a floor |
| EP-285 | the bound is 89 and no whole-tree score exists; the lane that would produce one times out at its 30-minute cap |
| EP-287 | the rule is read and recorded now; no command prints the number |
| EP-297 | status draft, no plan, no approval record, and its register shipped anyway |
| EP-298 | no check reads the spec-011 precedent |
| EP-300 | one home is proven for the surface list; spec 015 reads the same id as one proof record per capability, which does not exist |
| EP-301 | unknown-value-fails-closed is a schema property and a policy string, executed by nothing |
| EP-308 | a reason with no check |
| EP-311 | a reason with no check |
| EP-313 | a reason with no check |
| EP-316 | a reason with no check |
| EP-322 | the four-label legend is verbatim in index.html, but the document uses 19 distinct evidence-type values (CI, Council, Codigo, Decision, Corpus… |
| EP-324 | 15-skill target enforced (tests/test_capabilities.py:237); hooks/change_scope_guard.py:52 blocks a 4th file with no plan; "approved" is never… |
| EP-325 | docs/adr/0006-governed-mission.md claimed by tests/test_p0_completeness.py (passes); intent-to-evidence span only P0-deep (untestable as stated) |
| EP-326 | ai-security/ai-test/ai-report declared in policy/capabilities.toml with no bodies; accessibility and coordination absent entirely from the tree |
| EP-328 | outcome-first CLI landed (policy/outcome-v1.schema.json, docs/adr/0007-cli-contract.md, tests/test_outcomes.py); no P1 adapters, no 7 skills,… |
| EP-332 | ai-advise excluded by the closed allowed_ids (tests/test_capabilities.py:237 passes); nothing shows its heuristics landed in ai-review or ai-spec |
| EP-333 | ai-animation is in policy/capabilities.toml:142 and allowed_ids, but no .agents/skills/ai-animation exists and spec-010 defers it to P2, not "… |
| EP-334 | ai-autopilot excluded from allowed_ids (test passes); the "control plane as code, Git and CI" half is absent — no coordination code (untestabl… |
| EP-338 | policy/capabilities.toml:75 declares ai-build; no SKILL.md; preflight to CAPABILITY_ENFORCEMENT_UNAVAILABLE (tests/test_capabilities.py:359-393) |
| EP-343 | policy/capabilities.toml:128 declares ai-design; no SKILL.md, no a11y evidence; body is P2 per spec.md:287 (untestable as stated) |
| EP-344 | ai-docs absent; ai-review/references/ holds 5 lenses, none for docs; ai-ship only step 3 changelog (untestable as stated) |
| EP-345 | `ai-eng report issue` exits 1 "INCOMPLETE ... planned for P2" (src/ai_engineering/report.py:64-70); capabilities.toml:223 declares digest+issue |
| EP-346 | ai-explain absent; .agents/skills/ai-explore/SKILL.md carries no non-technical audience mode, and nothing executes the body |
| EP-352 | ai-learn absent; .agents/skills/ai-note/SKILL.md is the store but nothing executes it; no memory system in the tree |
| EP-354 | MCP tool results already reach injection_guard (hooks/chain.py:47, tests/test_hooks.py:554), but ai-security has no body to hold the mode |
| EP-357 | ai-onboard absent; `ai-eng init`/`doctor` execute, but ai-explore's tour clause is unexecuted prose in its SKILL.md |
| EP-361 | ai-pr absent; .agents/skills/ai-ship/SKILL.md:34-41 covers commit, PR, changelog and gates as prose nothing executes |
| EP-364 | ai-reliability-eval absent; justfile `check` runs no skill eval, only contract.audit's format check (test_every_skill_meets_the_contract) |
| EP-366 | ai-resolve-conflicts absent; .agents/skills/ai-debug/SKILL.md:40 "## Conflicts" is unexecuted prose; ai-ship says nothing about conflicts |
| EP-367 | ai-review kept (SKILL.md:32, five references, no per-reviewer template) but no frontend reference exists in .agents/skills/ai-review/references/ |
| EP-370 | capabilities.toml:156 declares ai-security; no SKILL.md, threat model, native scanner or SBOM (git grep sbom to tests/test_p0_completeness.py… |
| EP-373 | ai-simplify absent; ai-review/references/ has 5 lenses, none for simplification; "smaller version" only in the skill description |
| EP-382 | policy/capabilities.toml:111 declares ai-test; no SKILL.md; preflight to CAPABILITY_ENFORCEMENT_UNAVAILABLE; body is P2 (spec.md:287) |
| EP-383 | no ai-verify skill (capabilities.toml:188 declares it only); the four states live in policy/check-evidence-v1.schema.json; tests/test_evidence… |
| EP-385 | ai-visual absent; ai-design has no body either (capabilities.toml:128 declares it only), so no handler owns the behaviour |

### The 54 NO-EVIDENCE

| id | why it is not proven |
|---|---|
| EP-014 | no /ai-* router generator anywhere; wiring.py writes skills links and guard entries only |
| EP-017 | as EP-014, no router artifact is tracked |
| EP-018 | nothing executes or receipts a $ai-* invocation; codex-cli reads SURFACE_RECEIPT_MISSING |
| EP-026 | nothing counts routing collision; no threshold, no prompt set, no runner |
| EP-029 | no approver, no format, no location and no command for a routing evaluation |
| EP-042 | no threat model and no stack manifest exist |
| EP-043 | no SARIF anywhere; no detector module |
| EP-047 | no SBOM, no CycloneDX, no SPDX; the release workflow has no SBOM step |
| EP-053 | not among the thirteen rows; nothing computes it |
| EP-055 | no labelled defect corpus and no bar |
| EP-056 | no counter for escalations anywhere |
| EP-059 | no sequential baseline was recorded and no parallel execution exists to compare |
| EP-061 | no benchmark; its second command is an assertion |
| EP-062 | unfalsifiable as written; its recordable half is EP-066 |
| EP-063 | unfalsifiable; names no observable |
| EP-064 | traceability appears nowhere in the tree |
| EP-068 | no rubric; the criteria are one line each in AGENTS.md |
| EP-073 | unfalsifiable; left to a person |
| EP-087 | a comparative claim with no runnable legacy and no benchmark |
| EP-093 | stable baseline is undefined and unowned |
| EP-117 | no baseline, no sample, no margin and no evaluation runner |
| EP-122 | no comparison against a reference was defined or run; absorption applied as the fail-closed default |
| EP-140 | (untestable as stated) architectural claim; nothing in the tree checks it |
| EP-160 | no skill ships a handler — `.agents/skills/*/` holds SKILL.md only; nothing measures context saving (untestable as stated) |
| EP-180 | no plan schema exists; no claimed_paths, base_sha, deps or writer role field anywhere |
| EP-183 | nothing opens a draft pull request; claim.take pushes a ref and stops |
| EP-205 | as EP-014 |
| EP-207 | no codex-app row in policy/surfaces.toml and no refusal any check can read |
| EP-212 | as EP-014 |
| EP-213 | no agents/openai.yaml, no /skills discovery proof |
| EP-239 | two real directions only where there is a material visual decision is in no file |
| EP-241 | only-when-it-reduces-uncertainty is in no file |
| EP-243 | the absorb list is in no file |
| EP-244 | the optional list is in no file |
| EP-245 | styles-not-defaults is in no file |
| EP-246 | the rejected list did not land as a negative corpus |
| EP-252 | as EP-122 |
| EP-259 | the spec holds no plan, no BDD examples and no detector |
| EP-260 | KISS appears once as doctrine text; no rubric, no evidence ids |
| EP-262 | none of the nine detector classes exists as code |
| EP-280 | the release attests dist/* and computes no digest; no consumer verification command exists |
| EP-281 | the adversarial runner writes its own receipts; no attestation, scan or eval receipt exists |
| EP-289 | no eval runner, corpus or approver |
| EP-291 | no BDD criterion exists |
| EP-292 | critical journey appears only inside the register |
| EP-293 | AAA appears only in the design skill and the register |
| EP-302 | nothing can check that a test was red first; it stays a review criterion |
| EP-303 | no BDD example exists to require |
| EP-305 | a judgement no gate holds |
| EP-314 | a reason with no check, and none is possible: a council arrives as an argument |
| EP-319 | a reason with no check |
| EP-320 | a reason with no check |
| EP-329 | no DAG, checkpoint or merge_group anywhere; tests/test_p0_completeness.py:106 lists merge_group as a marker P0 may not claim (untestable as st… |
| EP-330 | no ai-security skill, no SBOM step in .github/workflows/release.yml (attestation only, line 102), no pilot artefact |

## Measured a third time, at `1d659572`

Twenty-one commits after the pass above, four read-only auditors re-ran every requirement
the record held as unproven, and a fifth tried to falsify the eleven this session claimed to
have closed. The arithmetic below is computed from their three tables rather than added up
by hand, because adding 33 newly proven to a standing 236 would have counted eleven of them
twice — the auditors re-measured eleven the baseline had already proven, and a total that
rounds in its own favour is what a ceiling and an audit both exist to prevent.

| verdict | first pass | second pass | third pass |
|---|---:|---:|---:|
| PROVEN | 192 | 236 | **258** |
| INCOMPLETE | 139 | 124 | 92 |
| NO-EVIDENCE | 54 | 22 | 17 |
| BLOCKED BY CONSTRUCTION | — | — | **14** |
| CONTRADICTED | 0 | 3 | **4** |
| total | 385 | 385 | 385 |

160 of the 385 were re-measured; the 225 nobody re-measured keep the verdict and the evidence
they already had. `evolution-proposal` is **67.0% proven**. It is not implemented.

`BLOCKED BY CONSTRUCTION` is new and it is not a softer INCOMPLETE. It names the fourteen
that cannot move from inside this repository at all: a published release, a merge queue that
the ruleset API says is not configured, an external organisation, a real pilot with a
sequential baseline, or an editor actually invoking a denial. Separating them says which of
the 127 unproven are work and which are circumstances.

### Five of the eleven this session claimed were overclaims

Found by one reader comparing what was built against what was asked, which no gate in this
repository could have done. `EP-373`, `EP-332`, `EP-352`, `EP-364`, `EP-262` and `EP-026`
stand, each falsified first — the verifier deleted a clause, or mutated a copy of the corpus,
and watched the check go red.

The other five did not:

- **`EP-135`** asks that *the surfaces* show the skills by the five phases. They are shown by
  `just skilleval`, so the person who meets the map is a developer running the gate and never
  a user of the wheel. `phase` appears nowhere in `src/`, `hooks/`, `surfaces/` or the README.
- **`EP-042`** has two halves. The coverage half landed. The threat model does not exist —
  this framework demands one from every user in `ai-security` and has never written its own.
- **`EP-043`** asks for a native stdlib-first scanner over skills and policy with its own
  adversarial fixtures. Reading other engines' SARIF is a different thing. The half that was
  refused is registered as `EP-262` with what would reopen it; the half that was not built is
  not closed.
- **`EP-083`** and **`EP-117`** ask for golden cases, cross-model replay as advisory, and
  blocking only after a stable baseline. The harness that shipped is deterministic, records
  no baseline and blocks on its first run.

The shape is the one this product is named after, committed by the session auditing it:
build the half you know how to build, and call the requirement closed.

### A fourth contradiction, on a decision already taken

`EP-074` joins `EP-333`, `EP-382` and `EP-383`: it asks for `ai-test` and `ai-verify` as
first-class skills, and a passing gate asserts both must not exist. That is the same recorded
decision seen from a fourth angle, and it is deliberate. What changed since the second pass is
that the two files no longer disagree silently —
`test_every_declared_capability_has_a_skill_or_names_where_its_work_went` reads the manifest
and the skill tree together and requires the homeless set to be exactly the absorption map.

### Two things the auditors found that nobody asked for

`policy/pilot-register.toml` justifies `skill_eval_delta` having no instrument with "there is
no evaluation runner, no approved corpus". That was true until this session and is now false:
there is a runner inside `just check` and 160 labelled cases. A stale reason inside a
governed register reads as though nobody had built the thing, and the register is the file
this repository points at to prove it is honest about what it cannot measure.

`scan.cross_check` is executed by its own test and by nothing in the product. Written and not
wired, which is the second-commonest finding in this whole record.

### After the third pass: what two block reviews changed

The count above was measured at `1d659572`. Eight commits later it is **259 of 385**, and the
one that moved is `EP-065`: it was in the ungated register as a judgement no gate could hold,
and its own reopening condition — that the production-ready boxes become the list of promises
— had fired before the row was written. `readiness.BOXES` is eight rows in code and every one
resolves to a receipt with an expectation and an age. The row is out and the binding is
executed both ways.

Nothing else moved, and that is the honest number. What did happen is that two independent
block reviews found nine defects between them in work this session had already called done:

- a coverage control permanently red for any repository with a package in a subdirectory;
- a SARIF reader taking an exception out through the security gate instead of a verdict;
- the adapter translation table read in the opposite direction to its own schema, which would
  have emptied `tool_name` on every surface at once;
- that same table then rewriting one surface's tool arguments on every other surface;
- a threat-model reader taking an exception out through the gate on the likeliest typo its
  format has — the same defect as the second item, committed while fixing it;
- and four checks that could not fail: a lens test satisfied by a word anywhere in the file, a
  guard-to-boundary test that only saw handlers with the right suffix, an orphan test that
  passed the two purest instances of what it exists to find, and a length compared to itself.

Every one was found by executing, none by re-reading, and none of them by a gate. That is what
`docs/adr/0011` proposes to make a rule rather than a habit, and it is still `proposed`.

## The other report, measured at last

Three passes had measured `evolution-proposal` and none had re-measured
`process-optimization-research`. Reading it is not measuring it, which is what this record
spends 500 lines telling other people. A fifth auditor derived the commitments from the
document itself — the `PO-01..PO-26` numbering exists in no file in this repository, so it
was re-derived rather than inherited — and measured each against the tree and against how
this session actually worked.

**10 PROVEN · 4 FAILED · 8 INCOMPLETE · 4 NO-EVIDENCE.**

The four that FAILED are all this session's, and three of them are the same fact:

- **One primary home per commit.** The widest commit touches **nineteen** files across
  `hooks/`, the justfile and fourteen `src/` modules. The figure this record published
  earlier — eleven — understates it by eight, and the owner's written exception covers a
  commit moving a count or a ceiling another check forces, not this.
- **One `just check` per block, and a second only if the first failed.** The gate ran roughly
  once per commit: about 118 runs against two recorded block reviews. Slower and stricter
  than the cadence rather than looser, which is the honest direction to deviate in, and still
  not the commitment the arithmetic was built on.
- **The writer continues without the full gate.** Same fact from the other side.

And one that is worse than a failure, because nothing can contradict it: **the run count is
unauditable from this tree.** `.ai-engineering/` is gitignored and the workflow does not
trigger on a push to this branch, so no receipt exists on either side. The only source for
how many times the gate ran is this record's own prose. A number nobody can check is the
shape this whole product exists to refuse, and it is now in the product's own audit.

Two more worth naming. The six-field block hand-off the plan requires — base, final HEAD,
reviewed commits, related-suite output, reviewer disposition, gate output — **is written in
no commit on this branch**, including the two block reviews that did happen. And red-first
cannot be shown by this tree at all, which the record already says under `EP-302`; it is a
review criterion, and the reviews were the thing that did not happen.

The one live rule-8 breach the same audit found — a shipped specification carrying its
owner's home directory — now has a control that would have refused it, and the breach is
named in that control's exemption list rather than quietly fixed in a record this project
does not rewrite.

## The two block hand-offs, written late

`specs/010/plan.md` requires a block to close with a hand-off carrying six things: the base,
the final HEAD, the commits reviewed, the related-suite output, the reviewer's disposition,
the repair commits and the gate output. The audit above found it written in no commit on this
branch, including the two block reviews that did happen. Here they are.

They are late and say so. A hand-off written after the fact cannot prove the freeze it claims
— nobody can now show that writes stopped while the reviewer read — so what these record is
what is checkable from the tree: the range, the commits, the findings and the repairs. The
freeze is asserted and not proven, which is the honest half of writing this down at all.

### Block E — requirement closure

| field | value |
|---|---|
| base | `7d5ab1fb` |
| final HEAD | `2dde4e5c` |
| commits reviewed | `2608176c`, `2b7357cb`, `a2b3441f`, `1aa7c649`, `2dde4e5c` |
| related suite | the full suite, green at the close |
| reviewer disposition | nine findings; two blocking |
| repair commit | `fc9704d6` |
| gate | `just check` green after the repair |

The two blocking findings were a coverage control permanently red for any repository with a
package in a subdirectory, and a SARIF reader taking an exception out through the security
gate instead of returning a verdict. Neither was reachable by re-reading.

### Block F — the policy and threat-model work

| field | value |
|---|---|
| base | `f22d9d9f` |
| final HEAD | `b27a418f` |
| commits reviewed | `5f411f78`, `ee61e082`, `976474b2`, `b27a418f`, with `a68275e2` for context |
| related suite | the full suite, green at the close |
| reviewer disposition | four blocking findings, plus one row of the register sent back |
| repair commits | `04bc1642`, `e3c779dd` |
| gate | `just check` green after each |

Two of the four were in checks written one commit earlier to catch exactly that class. The
row sent back was `EP-065`, filed as ungatable while its own reopening condition had already
fired — the register becoming a place to put work, one session after it was built.

Both hand-offs are missing the same field for the same reason: **no independent record of the
gate run.** The receipts are gitignored and the workflow does not trigger on a push to this
branch, so "green after the repair" is this record's word. That is the finding above, and
writing the hand-off does not close it.
