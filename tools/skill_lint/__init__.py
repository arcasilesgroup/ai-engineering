"""skill_lint — CLI shim that wires app + infra for the conformance lint.

Console entry: ``python -m skill_lint`` and ``skill_lint`` (registered
in ``pyproject.toml`` ``[project.scripts]``).
"""

from __future__ import annotations
