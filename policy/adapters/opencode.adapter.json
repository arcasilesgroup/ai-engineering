{
  "schema": "urn:ai-engineering:surface-adapter:1",
  "schema_version": "1",
  "surface_id": "opencode",
  "adapter_version": "1",
  "detection": {
    "signal": "~/.config/opencode, created by OpenCode itself; the plugin we install lives under it",
    "written_by_us": false
  },
  "translations": {
    "payload_field": {
      "tool_name": "tool",
      "tool_input": "args"
    },
    "lifecycle_event": {
      "PreToolUse": "tool.execute.before"
    },
    "exit_meaning": {
      "0": "allow",
      "2": "deny"
    },
    "reply": {
      "allow": "return, and the tool call proceeds",
      "deny": "throw inside tool.execute.before, which kills the tool call and not the turn",
      "error": "throw, which OpenCode reports to the model as a per-tool error"
    }
  },
  "proof": {
    "receipt_id": "opencode.enforcement"
  },
  "trust": {
    "required": false
  },
  "deny_protocol": "throw-in-tool-execute-before"
}
